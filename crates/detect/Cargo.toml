[package]
name = "rotten-apple-detect"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Host inspection for rotten-apple — distro, UEFI, IOMMU, GPU, disk space, etc."

[dependencies]

[dev-dependencies]
tempfile = "3"
